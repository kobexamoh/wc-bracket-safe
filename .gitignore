# Secrets - CRITICAL
.env
.env.local
.env.*.local

# Dependencies
node_modules/
package-lock.json
yarn.lock

# Build output
dist/
build/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*

# Project notes (local only - avoids exposing file paths)
PROJECT_STATE.md

# Personal / career docs (local only)
docs/MANAGER_GROWTH.md
docs/PORTFOLIO_CASE_STUDY.md

# User testing / feedback notes (local only - avoids exposing notes + file paths)
user-feedback-notes/

# Cursor handoff (local-only context for resuming in Cursor)
docs/CURSOR_HANDOFF.md

# Portable AI context layer (Atlas) - local only: personal/career notes + project paths
# Canonical home is ~/Desktop/Atlas (kept in iCloud). This repo only holds a gitignored
# symlink to it, plus a local-only Cursor rule that points at it.
atlas
personal-context
.cursor/rules/atlas.mdc

# Original/reference scratch (local only - the pre-modular wc-bracket.html, kept for the knockout layout)
reference/
